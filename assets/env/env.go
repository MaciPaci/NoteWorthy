package env

// ConfigFilePath represents the path to the config.json file
var ConfigFilePath = "./assets/config/config.json"

// Prefix represents the input prefix the bot reacts to
var Prefix = "!"
